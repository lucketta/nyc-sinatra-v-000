<h1>Edit a Figure:</h1>

<form action="/figures/<%= @figure.id %>" method="post">
  <input type="hidden" name="_method" value="patch" id= "hidden">
  <h3>Edit Figure Name: <input type="text" name="figure[name]" id="figure_name" placeholder="<%= @figure.name %>"></h3>

  <h3>Edit Landmarks: </h3>
  <% Landmark.all.each do |landmark| %>
    <input type="checkbox" name="figure[landmark_ids][]" value="<%= landmark.id %>" id="landmark_<%= landmark.id %>" <%= 'checked' if @figure.landmarks.include?(landmark) %>><%= landmark.name %></input>
  <% end %>
  <h3>Edit Titles: </h3>
  <% Title.all.each do |title| %>
    <input type="checkbox" name="figure[title_ids][]" value="<%= title.id %>" id="title_<%= title.id %>" <%= 'checked' if @figure.titles.include?(title) %>><%= title.name %></input>
  <% end %>

  <br><br>
  <h3>Edit Landmarks/Title: </h3>
  <h3>Edit Landmark: <input type="text" name="landmark[name]" id="new_landmark"></h3>

  <h3>Edit Title: <input type="text" name="title[name]" id="new_title"></h3>
  <input type="submit" value="Edit Figure">
</form>
