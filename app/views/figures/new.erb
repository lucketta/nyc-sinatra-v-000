<h1>Create a New Figure:</h1>

<form action="/figures" method="post">
  <h3>Figure Name: <input type="text" name="figure[name]" id="figure_name"></h3>

  <h3>Choose Landmarks: </h3>
  <% Landmark.all.each do |landmark| %>
    <input type="checkbox" name="figure[landmark_ids][]" value="<%= landmark.id %>" id="landmark_<%= landmark.id %>"><%= landmark.name %></input>
  <% end %>

  <h3>Choose Titles: </h3>
  <% Title.all.each do |title| %>
    <input type="checkbox" name="figure[title_ids][]" value="<%= title.id %>" id="title_<%= title.id %>"><%= title.name %></input>
  <% end %>

  <br><br>
  <h3>Create Your Own Landmarks/Title: </h3>
  <h3>Landmark Name: <input type="text" name="landmark[name]" id="new_landmark"></h3>

  <h3>Title Name: <input type="text" name="title[name]" id="new_title"></h3>
  <input type="submit" value="Create New Figure">
</form>
